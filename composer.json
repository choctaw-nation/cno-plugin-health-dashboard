{
	"name": "choctaw-nation/cno-plugin-health-dashboard",
	"description": "A plugin that displays a health dashboard with virality charts for common diseases.",
	"type": "project",
	"version": "1.1.0",
	"autoload": {
		"classmap": [
			"inc/"
		]
	},
	"autoload-dev": {
		"classmap": [
			"tests/"
		]
	},
	"config": {
		"allow-plugins": {
			"composer/installers": true,
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	},
	"scripts": {
		"phpcs": "phpcs inc/ cno-plugin-health-dashboard.php --extensions=php",
		"phpcbf": "phpcbf inc/ cno-plugin-health-dashboard.php --extensions=php",
		"test": "phpunit",
		"install-tests": [
			"chmod +x bin/local-mysql-socket.sh",
			"bash -lc 'SOCK=\"$(bin/local-mysql-socket.sh)\"; bash -x bin/install-wp-tests.sh wordpress_test root root \"$SOCK\" latest'"
		]
	},
	"require-dev": {
		"doctrine/instantiator": "^1",
		"phpunit/phpunit": "^9",
		"wp-coding-standards/wpcs": "*",
		"yoast/phpunit-polyfills": "^4.0"
	}
}
